apiVersion: batch/v1
kind: Job
metadata:
  name: agent-alpha-job
  namespace: agents
  labels:
    app: agent-alpha
spec:
  template:
    metadata:
      labels:
        app: agent-alpha
    spec:
      restartPolicy: OnFailure
      containers:
      - name: agent-alpha
        image: agent-alpha:latest
        command: ["dotnet", "AgentAlpha.dll", "test"]
        env:
        - name: MCP_SERVER_URL
          value: "http://mcpserver-service:3000"
        - name: REST_BASE_URL
          value: "http://approval-service-service:5000"
        envFrom:
        - configMapRef:
            name: agents-config
        - secretRef:
            name: agents-secrets
        resources:
          requests:
            memory: "128Mi"
            cpu: "100m"
          limits:
            memory: "256Mi"
            cpu: "200m"
---
# Alternative: CronJob for scheduled agent runs
apiVersion: batch/v1
kind: CronJob
metadata:
  name: agent-alpha-cronjob
  namespace: agents
  labels:
    app: agent-alpha-cron
spec:
  schedule: "0 */6 * * *"  # Run every 6 hours
  jobTemplate:
    spec:
      template:
        metadata:
          labels:
            app: agent-alpha-cron
        spec:
          restartPolicy: OnFailure
          containers:
          - name: agent-alpha
            image: agent-alpha:latest
            command: ["dotnet", "AgentAlpha.dll", "test"]
            env:
            - name: MCP_SERVER_URL
              value: "http://mcpserver-service:3000"
            - name: REST_BASE_URL
              value: "http://approval-service-service:5000"
            envFrom:
            - configMapRef:
                name: agents-config
            - secretRef:
                name: agents-secrets
            resources:
              requests:
                memory: "128Mi"
                cpu: "100m"
              limits:
                memory: "256Mi"
                cpu: "200m"